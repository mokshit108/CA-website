/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // If you're using images, you need this configuration
    images: {
      unoptimized: true,
    },
  };
  
export default nextConfig;
  